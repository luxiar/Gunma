<div class="text-center mb-5">
  <h1>日報一覧</h1>
  <%= link_to "新規日報", new_daily_report_path, class: 'btn btn-outline-info' %>
</div>

<div class="row row-cols-1">
  <div class="col-md-8 offset-md-2">
    <% @daily_reports.each do |daily_report| %>
      <div class="card mb-2">
        <div class="card-body">
          <h5 class="card-title"><%= link_to daily_report.title, daily_report, class: "link-dark link-underline link-underline-opacity-0 stretched-link" %></h5>
          <p class="card-text">
            <small class="text-muted">
              <%= link_to daily_report.user.full_name, user_path(daily_report.user), class: "link-secondary link-offset-2 link-underline link-underline-opacity-0 link-underline-opacity-75-hover position-relative z-1" %>
              <%= l daily_report.created_at %>
            </small>
          </p>
        </div>
      </div>
    <% end %>
    <div class="mt-3 mb-5">
      <%= paginate @daily_reports %>
    </div>
  </div>
</div>
