<h1>Editing user</h1>

<%= render "form", user: @user %>

<br>

<div>
  <%= link_to "Show this user", @user %>

  <% if can? :read, @user %>
    <%= link_to "Back to users", users_path %>
  <% end %>
</div>
